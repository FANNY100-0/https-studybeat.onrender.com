# StudyBeat
Plataforma de organización académica integrada con descubrimiento musical.

## Instalación
1. `pip install -r requirements.txt`
2. `python app.py`

## Despliegue en Render
- Build Command: `pip install -r requirements.txt`
- Start Command: `gunicorn app:app`
